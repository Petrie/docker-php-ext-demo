// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("calc", "for calc support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("calc", "enable calc support", "no");

if (PHP_CALC != "no") {
	EXTENSION("calc", "calc.c", PHP_EXTNAME_SHARED, "/DZEND_ENABLE_STATIC_TSRMLS_CACHE=1");
}

